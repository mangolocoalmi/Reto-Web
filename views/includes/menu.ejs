<header class="header">
    <div class="logo">
        <img src="/images/logo2.png" alt="logo">
    </div>
    <div id="toggle">
        <i class="fas fa-bars"></i>
    </div>
    <nav class="nav">
        <ul class="menu">
            <li <% if (menuId === 'admin') { %> class="active" <% } %> id="home"><a href="/admin">INICIO</a></li>
            <li <% if (menuId === 'registro') { %> class="active" <% } %> id="registro"><a href="/registro">REGISTRO</a></li>
            <% if(user) { %>
                <li <% if (menuId === 'estadistica') { %> class="active" <% }  %> id="admin"><a href="/estadisticas">ESTADISTICAS</a></li>
                <li id="logout"><a href="/logout">CERRAR SESION</a></li>
            <% } %>
        </ul>
    </nav>
</header>